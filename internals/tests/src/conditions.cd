func getSomething(something i32) i1 {
    return something == 2 && something >= 2 && something <= 2
}

func main() {
    truth := 1 as i1
    wow := truth && truth
    if truth && truth && truth && truth{

        if !truth && truth && truth && truth && truth && truth {

        } else if truth && truth {
            
        } else if !truth {
            @print("this is bad")
        } else {
            @print("this is also really bad")
        }

    } else {
        @print("wow this is bad!")
    }
    if !wow {
        @print("also wow bad")
    }
    s := getSomething(2)
    if !s {
        @print("-_-")
    }

    if 0 as i1 || 1 as i1 {
        
    } else {
        @print("pretty bad!")
    }

    if !( wow && wow ) || !( wow ) {
        @print("bad.")
    } else if !(wow && wow) || (wow) {
    } else {
        @print("bad!!.")
    }
}
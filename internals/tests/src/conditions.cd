func getSomething(something i32) i1 {
    return something == 2 && something >= 2 && something <= 2
}

func main() {
    truth := 1 as i1
    wow := truth && truth
    if truth && truth && truth && truth{

        if !truth && truth && truth && truth && truth && truth {

        } else if truth && truth {
            
        } else if !truth {
            @println("this is bad")
        } else {
            @println("this is also really bad")
        }

    } else {
        @println("wow this is bad!")
    }
    if !wow {
        @println("also wow bad")
    }
    s := getSomething(2)
    if !s {
        @println("-_-")
    }

    if 0 as i1 || 1 as i1 {
        
    } else {
        @println("pretty bad!")
    }

    if !( wow && wow ) || !( wow ) {
        @println("bad.")
    } else if !(wow && wow) || (wow) {
    } else {
        @println("bad!!.")
    }
}